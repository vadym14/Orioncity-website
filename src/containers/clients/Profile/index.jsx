import React from "react";
import "./index.scss";

import ProfileContent from "../../../components/clients/Profile";

const ProfilePage = (props) => {
  return <ProfileContent />;
};

export default ProfilePage;
